#shape of the data set
print(iris.shape)
#output will be in (rows,columns)


print(iris['species'].value_counts())
print(iris.columns)
print(iris.values)


#to get the information of the data set
print(iris.info())
print(iris.describe())




# here columns ('sepal_length', 'sepal_width', 'petal_length', 'petal_width') will be X
# and column('species') will be Y

X = iris.iloc[:,:4] #first four coumlns
Y = iris.iloc[:,-1] #last column
print(X)
print(Y)
